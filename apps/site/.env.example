# 本地前台跨端口访问后端时设置；生产同源代理可留空
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# 公开构建保持 false；内部专用构建才同时开启下面两项
NEXT_PUBLIC_ENABLE_INTERNAL_VIEW=false
DOCS_BUILD_AUDIENCE=external

# 内部全文接口的 Bearer Token；不配置时接口返回 404
INTERNAL_DOCS_TOKEN=
